/* eslint-disable react/no-multi-comp */
/* eslint-disable no-shadow */
/* eslint-disable react/react-in-jsx-scope */
/* eslint-disable react/jsx-no-target-blank */
/* eslint-disable no-unused-vars */
//getAll names
/* async function getAll(req, res) {
    try {
        const threes = await Arbustum.find();
        res.json(threes.map(three => three.nom_complet));
    } catch (error) {
        res.send(error);
        // expected output: ReferenceError: nonExistentFunction is not defined
        // Note - error messages will vary depending on browser
    }
} */
/* import faker from "faker";
function fakerName() {
    const fakeName = faker.name.lastName;
    return fakeName;
}

//fake name
/* function fakerName() {
    const fakeName = faker.name.findName();
    return fakeName;
} */

//fake name
/*
exports.fakerName = function () {
    const fakeName = faker.name.findName();
    return fakeName;
};

/* exports.wiki = function (wiki) {
    return (
        <a
            href={`https://en.wikipedia.org/wiki/${wiki}`}
            target={"_blank"}
            rel={"noopener"}
            className={"icon randomIcon"}>
            {"Lien wikipédia "}
        </a>
    );
};
function sheete() {
    const sheetes = 0;
    let now = new Date();
    now.setMinutes(now.getMinutes() + 30); // timestamp
    now = new Date(now); // Date object
    console.log(now);
} */

/* function fakerName() {
    const fakeName = faker.name.findName();
    return fakeName;
} */

/* function show(req, res) {
    try {
        const juste = sheete();
        console.log(`show${juste}`);
    } catch (error) {
        console.log(error);
        // expected output: ReferenceError: nonExistentFunction is not defined
        // Note - error messages will vary depending on browser
    }
}*/
